#import <UIKit/UIKit.h>

/// Màn "Luật" (FR-7): bảng khoảng cách tối thiểu, mức phạt, bối cảnh,
/// phiên bản số liệu đang dùng và miễn trừ trách nhiệm.
@interface KCLegalViewController : UIViewController
@end
